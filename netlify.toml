[build]
  # CRITICAL: Tell Netlify where the functions are located
  functions = "netlify/functions"
  
[functions]
  # CRITICAL FIX: Use esbuild, which is more reliable for external fetch calls
  node_bundler = "esbuild"
  node_version = "18"
