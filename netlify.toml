[build]
  command = "echo 'Netlify build starting...'"
  functions = "netlify/functions"
  
[functions]
  node_bundler = "esbuild"
  node_version = "18"

# This block ensures the build environment sets the key correctly
[build.environment]
  NODE_VERSION = "18"
