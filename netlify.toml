[build]
  publish = "." 
  
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Universal Redirect: Forces all requests to index.html to fix page loading
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
