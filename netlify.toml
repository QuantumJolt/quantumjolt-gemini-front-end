[build]
  # Tell Netlify where the functions are located (using the default, zero-config setting)
  functions = "netlify/functions"

# Optionally specify a Node version for stability
[build.environment]
  NODE_VERSION = "18"
